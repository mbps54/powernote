all:
  children:
    powernote:
      hosts:
        powernote-prod:
          ansible_host: YOUR_SERVER_IP
          ansible_user: root
          ansible_port: 65022
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
